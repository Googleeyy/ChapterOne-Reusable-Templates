name: Reusable Build & Trivy Scan

on:
  workflow_call:
    inputs:
      image_name:
        required: true
        type: string
      tag:
        required: true
        type: string
      is_production:
        required: true
        type: string
      working_directory:
        required: false
        type: string
        default: '.'
      docker_context:
        required: false
        type: string
        default: '.'
      dockerfile_path:
        required: false
        type: string
        default: 'Dockerfile'
      runtime:
        required: true
        type: string
      java_version:
        required: false
        type: string
        default: '21'
      node_version:
        required: false
        type: string
        default: '20'
      trivy_severity:
        required: false
        type: string
        default: 'CRITICAL,HIGH'
      trivy_ignore_unfixed:
        required: false
        type: string
        default: 'false'
    secrets:
      DOCKER_USERNAME:
        required: true
      DOCKER_PASSWORD:
        required: true
      BREVO_API_KEY:
        required: false
      NOTIFY_EMAIL:
        required: false
    outputs:
      image_full:
        value: ${{ jobs.build.outputs.image_full }}
      trivy-critical:
        value: ${{ jobs.build.outputs.trivy_critical }}

jobs:
  build:
    name: Build & Scan
    runs-on: ubuntu-latest
    outputs:
      image_full: ${{ steps.build.outputs.image_full }}
      trivy_critical: ${{ steps.trivy-check.outputs.critical_found }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Runtime Setup: Java ──────────────────────
      - name: Setup Java
        if: inputs.runtime == 'java'
        uses: actions/setup-java@v4
        with:
          java-version: ${{ inputs.java_version }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Build JAR
        if: inputs.runtime == 'java'
        working-directory: ${{ inputs.working_directory }}
        run: mvn package -DskipTests --batch-mode

      # ── Runtime Setup: Node ──────────────────────
      - name: Setup Node.js
        if: inputs.runtime == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node_version }}

      - name: Install & Build
        if: inputs.runtime == 'node'
        working-directory: ${{ inputs.working_directory }}
        run: |
          npm install
          npm run build

      # ── Docker Login ─────────────────────────────
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ── Docker Build ─────────────────────────────
      - name: Docker Build (Dev)
        if: inputs.is_production == 'false'
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }} -f ${{ inputs.dockerfile_path }} ${{ inputs.docker_context }}

      - name: Docker Build (Production with Cache)
        if: inputs.is_production == 'true'
        run: |
          # Try to pull previous latest for cache (ignore failure)
          docker pull ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:latest || true
          # Build with cache
          docker build --cache-from ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:latest -t ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }} -f ${{ inputs.dockerfile_path }} ${{ inputs.docker_context }}

      - name: Set Image Full Output
        id: build
        run: |
          echo "image_full=${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }}" >> $GITHUB_OUTPUT

      # ── Save & Upload Image ──────────────────────
      - name: Save Docker Image
        run: |
          docker save -o image.tar ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }}

      - name: Upload Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image
          path: image.tar
          retention-days: 1

      # ── Load for Trivy Scan ──────────────────────
      - name: Load Docker Image
        run: docker load -i image.tar

      # ── Trivy Scan (JSON for parsing) ────────────
      - name: Trivy Scan (JSON for Analysis)
        id: trivy-json
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }}
          format: 'json'
          output: 'trivy-results.json'
          exit-code: '1'
          ignore-unfixed: ${{ inputs.trivy_ignore_unfixed }}
          severity: 'CRITICAL,HIGH'

      - name: Check for Critical Vulnerabilities
        id: trivy-check
        run: |
          if [ -f trivy-results.json ] && grep -q '"Severity": "CRITICAL"' trivy-results.json; then
            echo "critical_found=true" >> $GITHUB_OUTPUT
            echo "🚨 CRITICAL vulnerabilities found!"
          else
            echo "critical_found=false" >> $GITHUB_OUTPUT
            echo "✅ No CRITICAL vulnerabilities found"
          fi

      # ── Trivy Scan (Table) ────────────────────────
      - name: Trivy Scan (Table Format)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }}
          format: 'table'
          exit-code: '1'
          ignore-unfixed: ${{ inputs.trivy_ignore_unfixed }}
          severity: ${{ inputs.trivy_severity }}

      # ── Trivy Scan (SARIF) ───────────────────────
      - name: Trivy Scan (SARIF Format)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          ignore-unfixed: ${{ inputs.trivy_ignore_unfixed }}
          severity: ${{ inputs.trivy_severity }}
      
      # ── Upload to GitHub Security ────────────────
      - name: Upload Trivy SARIF
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

      # ── Job Summary ──────────────────────────────
      - name: Generate Job Summary
        if: always()
        run: |
          echo "## 🐳 Build & Trivy Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Image** | ${{ secrets.DOCKER_USERNAME }}/${{ inputs.image_name }}:${{ inputs.tag }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Runtime** | ${{ inputs.runtime }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Is Production** | ${{ inputs.is_production }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Trivy Severity** | ${{ inputs.trivy_severity }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Critical Found** | ${{ steps.trivy-check.outputs.critical_found }} |" >> $GITHUB_STEP_SUMMARY

  # ─────────────────────────────────────────────
  # NOTIFY-TRIVY — Alert on Critical Vulnerabilities
  # ─────────────────────────────────────────────
  notify-trivy:
    name: Notify Critical Vulnerabilities
    needs: build
    if: always() && needs.build.outputs.trivy-critical == 'true'
    uses: ./.github/workflows/_notify-brevo.yml@main
    with:
      subject: "🚨 Trivy: Critical Vulnerabilities in ${{ inputs.image_name }}"
      body: "CRITICAL vulnerabilities found in ${{ inputs.image_name }} Docker image. Please review immediately."
      service_name: ${{ inputs.image_name }}
      artifact_name: ${{ inputs.image_name }}:${{ inputs.tag }}
      environment: ${{ inputs.is_production == 'true' && 'production' || 'development' }}
    secrets:
      BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
      NOTIFY_EMAIL: ${{ secrets.NOTIFY_EMAIL }}
